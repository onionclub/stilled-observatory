name = "stilled"
compatibility_date = "2026-05-29"
pages_build_output_dir = "./dist"
compatibility_flags = ["nodejs_compat"]

[env.production]
vars = { GHOST_URL = "", GHOST_CONTENT_API_KEY = "" }

# Stripe — set via `wrangler secret put` or Cloudflare dashboard
# STRIPE_SECRET_KEY    — sk_live_xxx (never commit this)
# STRIPE_WEBHOOK_SECRET — whsec_xxx (never commit this)

# For local dev, create a .dev.vars file:
# STRIPE_SECRET_KEY=sk_test_xxx
# STRIPE_WEBHOOK_SECRET=whsec_xxx


[[kv_namespaces]]
binding = "PAYMENT_LOG"
id = "00dcca40debe4ccfab5b82bf381453b0"
